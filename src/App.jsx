'use strict';

const contentNode = document.getElementById('contents');

class LoginForm extends React.Component {
  render() {
    return (
      <div>Placeholder for login form</div>
    )
  }
}

ReactDOM.render(<LoginForm />, contentNode);
